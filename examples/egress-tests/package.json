{
  "name": "egress-tests",
  "version": "1.0.0",
  "description": "Integration tests for egress interception",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0",
    "wrangler": "4.81.0"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20260405.1"
  }
}
